{
  "name": "free-storage-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --filter backend run build && pnpm --filter web run build",
    "start": "PORT=3000 pnpm --filter backend run start & pnpm --filter web dev"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  }
}
